# render artifacts (regenerated every run)
*.mp4
*-thumb.png
short-*.json
long-*.json
work/

# model downloads are cached by GitHub Actions (not committed)
models/

# re-auth token output — never commit
rt.bin.json

# python
__pycache__/
*.pyc

# local tooling (ffmpeg/ffprobe binaries installed via npm)
tools/node/

# macOS
.DS_Store
# house-voice models (huge) and the owner's reference recordings (private)
voice-openvoice/
voices/
